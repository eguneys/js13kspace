{
  "name": "js13k",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dist": "yarn -s build && zip -r dist.zip -j dist && echo `ls -lh dist.zip | cut -f5 -d ' '` bytes.",
    "start": "webpack serve --port=3000 --mode=development",
    "build": "webpack --mode=production",
    "levels": "node gen/levels.js"
  },
  "devDependencies": {
    "html-webpack-plugin": "^5.3.2",
    "pngjs": "^6.0.0",
    "webpack": "^5.50.0",
    "webpack-cli": "^4.8.0",
    "webpack-dev-server": "^4.0.0"
  }
}
